name: Sync to Gitee
on:
  push:
    branches: [ main ]

jobs:
  mirror:
    runs-on: ubuntu-latest
    steps:
      - name: Mirror to Gitee via HTTPS
        uses: Yikun/hub-mirror-action@v0.18.0  # 使用明确版本，避免 master 不稳定
        with:
          src: github/Li-xunwen
          dst: gitee/Li-xunwen
          mappings: |
            TalonTools-Overwatch=>talon-tools-overwatch
            intelligent-logistics-car=>intelligent-logistics-car
            RemoteControlCarRTCWeb=>remote-control-car-rtc-web
            xunwen=>xunwen
          clone_style: https
          push_style: https        # 👈 关键！强制推送也用 HTTPS
          dst_token: ${{ secrets.GITEE_TOKEN }}
          force_update: true
          static_list: ""